# Provenance

This is the canonical Chapter 5 TP4/TP8/TP16 run. It was generated on 2026-08-08
with workspace revision `57f201655f40`, generator revision `f757579fee53`, and PCM
revision `3179f0bb5e91` using the command recorded in `manifest.json`.

The manifest records hashes for the simulator, collective compiler, congestion-control
library and configuration, and every captured source input. The source snapshot under
`source_snapshot/` matches those hashes. The workspace dirty flag in the manifest is
caused only by a local `.claude/settings.local.json` file that is ignored on the host;
the generator and simulator submodules were clean.

The six measured rows are in `tables/results.csv`. `audits.json` contains the workload
and trace checks, and `thesis-assets/` contains the figure and tables generated from
the completed run.
