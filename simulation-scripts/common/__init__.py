# Shared library for simulation-scripts experiments (pure stdlib).
# Import as `from common import goal, paths, report, sim` after putting the
# simulation-scripts root on sys.path (see any experiments/*/run.py header).
